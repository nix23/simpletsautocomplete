import React from 'react';

import './Loader.scss';

// Css based spinner-loader with animation
export default () => {
  return (
    <div className="autocomplete__loader">
      <div className="loader"/>
    </div>
  );
};